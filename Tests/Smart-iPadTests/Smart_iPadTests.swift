import XCTest
@testable import Smart_iPad

final class Smart_iPadTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(Smart_iPad().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
